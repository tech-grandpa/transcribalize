# Transcribalize service dependencies

# Whisper transcription
faster-whisper>=1.2.1

# Web framework
fastapi>=0.139.2
uvicorn>=0.51.0
python-multipart>=0.0.26
websockets>=16.0

# Async file handling for large uploads
aiofiles>=25.1.0

# Audio processing for live transcription
# Numba 0.65.x (via librosa in the optional ASR stack) requires NumPy <2.5.
numpy>=2.4.4,<2.5

# VAD and optional ASR audio I/O use native TorchAudio extensions.
# Keep Torch and TorchAudio on the same release; cross-release pairs are unsupported.
torch==2.11.0
torchaudio==2.11.0

# LLM analysis (OpenRouter via LiteLLM)
litellm>=1.83.7
