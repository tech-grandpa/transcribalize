# Copy this file outside the public repo, fill in local values, then run:
#   set -a; source /path/to/deploy.env; set +a
#   ./scripts/deploy.sh

DEPLOY_HOST=deploy-host
DEPLOY_USER=deploy
DEPLOY_PATH=/srv/transcribalize
# COMPOSE_DIR=/srv/transcribalize/transcriber
# HEALTH_URL=http://localhost:8000/health
# MAX_RETRIES=30
# RETRY_DELAY=10
